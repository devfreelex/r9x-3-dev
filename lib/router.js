import { appCreator } from './component.js'

const routerFactory = () => {

    const _routes = {}

    const setRoutes = (routes) => {
        Object.assign()
    }

   return {
       setRoutes,
       init
   }
}

export { routerFactory }