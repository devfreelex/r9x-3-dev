export default ({ctx, css}) => css`
    ${ctx} .other-wrapper {
        display:block;
        float:left;
        width:100%;
        padding:15px;
        background:#fff;
        text-align:center;
    }
`