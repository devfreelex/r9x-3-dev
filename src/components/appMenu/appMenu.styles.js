export default ({ctx, css}) => css`
    ${ctx} .menu-wrapper {
        display:block;
        float:left;
        width:100%;
        background:#ff6600;
        padding:15px;
        color:#fff;        
    }
`