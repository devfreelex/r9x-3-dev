export default ({ ctx, css }) => css`
    ${ctx} .header-wrapper {
        display:block;
        float:left;
        width:100%;
        background:#000;
        padding:15px;
        color:#fff;
    }
`