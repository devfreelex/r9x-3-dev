export default ({ctx, css}) => css`
    ${ctx} .main-wrapper {
        display:block;
        float:left;
        width:100%;
        padding:15px;
        background:#f1f1f1;
    }

    ${ctx} * {
        box-sizing: border-box
    }
`   